单次运行指令 scrapy crawl dmoz
启动指令 python start.py

##在IDE中调试的
from scrapy.cmdline import execute
execute(['scrapy', 'crawl', 'dingdian'])

https://coinmarketcap.com/

https://etherscan.io

# 添加数据库字段 
alter table currencys add url varchar(255);

# 清空表 
truncate table `currencys`;
